server:
  port: 8000

spring:
  application:
    name: currency-exchange-service
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka